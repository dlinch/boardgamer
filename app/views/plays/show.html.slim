h1 = @play.game&.name
h3 = @play.updated_at.strftime("%A, %B %d, %Y")
h2 Players
- @play.players.each do |pl|
    p = pl.full_name

h2 Winner
p = @play.winner&.full_name

a href= edit_play_path(@play)
  button Edit
